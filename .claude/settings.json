{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "R=\"${CLAUDE_PROJECT_DIR:-$PWD}/graphify-out/GRAPH_REPORT.md\"; if [ -f \"$R\" ]; then echo \"MAPA (graphify): $(grep -m1 'nodes' \"$R\" | sed 's/^- //')\"; echo \"  Armado: $(date -r \"$R\" '+%d-%b %H:%M' 2>/dev/null). El CODIGO se refresca solo en cada commit.\"; echo \"  OJO: documentos, .md e imagenes NO entran en ese refresco -> correr '/graphify . --update' a mano.\"; else echo 'MAPA (graphify): NO armado en esta maquina -> correr /graphify . ; mientras tanto usar Grep/Read normalmente.'; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
